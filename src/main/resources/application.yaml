spring:
  application:
    name: coursy-platforms
  datasource:
    url: jdbc:postgresql://localhost:5432/platforms_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
auth:
  service:
    url: http://localhost:9000